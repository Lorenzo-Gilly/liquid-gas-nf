# ── JAX + CUDA ───────────────────────────────────────────────────────────────
# Install this FIRST, before running `pip install -r requirements.txt`
# Check your CUDA version with: nvidia-smi  (top-right corner)
#
#   pip install "jax[cuda12]==0.8.2"   # CUDA 12.x  (most common)
#   pip install "jax[cuda13]==0.8.2"   # CUDA 13.x  (new drivers)
#
# This installs jax + jaxlib + the matching CUDA plugins in one step.
# ─────────────────────────────────────────────────────────────────────────────

dm-haiku==0.0.16
optax==0.2.6
numpy==2.4.0
scipy==1.16.3
matplotlib==3.10.8
chex==0.1.91

# distrax — PyPI version is outdated, must install from git
distrax @ git+https://github.com/google-deepmind/distrax.git@0b590e967d707b3a112a9d3b21643a8556650b83
